extends layout 

block content 
  body
    h3 Object #{id} / 30      
    
    img(src='images/objects/#{objectId}.PNG' alt='Image' style='width: 200px; height: 200px;')
    br
    br

    form(action="/log/describe/#{id}", method="post", onsubmit="validateInput(event);")
      p 1. Have you seen this object before?
      
      label.radio
        input(type='radio' name='seen' value='yes') 
        span.black-text.radio-label Yes
      
      label.radio
        input(type='radio' name='seen' value='no')
        span.black-text.radio-label No
      
      br
      br
      p 2. If yes, what is it? If no, please provide a description of this object in a short sentence.
      input#description(type='text' name='description' placeholder="Describe this object in a short sentence.")
      
      input(type="hidden" name="id" value='#{id}')
      
      br
      br
      button.waves-effect.waves-light.btn Next
    
    script. 
        const id = #{id};
    script(src='javascripts/describe.js')
